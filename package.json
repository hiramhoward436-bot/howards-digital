{
  "name": "howards-digital",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply HD_DB --local",
    "db:migrate:remote": "wrangler d1 migrations apply HD_DB --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
